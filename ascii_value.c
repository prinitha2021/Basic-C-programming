#include <stdio.h> // Ascii value of a given character

int main()
{
    char c;
    printf("\nenter any character to find its Ascii value.");
    scanf("%c",&c);
    printf("\nThe Ascii value of %c is %d.",c,c);
    
    return 0;
}
